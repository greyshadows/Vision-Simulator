using UnityEngine;
using System.Collections;

public class redxSphereAudioSource : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	/*public  void mute1(bool status)
	{
		AudioSource audio;
		audio = GetComponent<OVRAudioSource>();
		if (status) 
			audio.mute = true;
		else
			audio.mute = false;
	}*/
}
